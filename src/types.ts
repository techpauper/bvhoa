export interface Subdivision {
  id: string;
  name: string;
  description: string;
  image: string;
  features: string[];
}

export interface Director {
  name: string;
  role: string;
  email: string;
  bio: string;
  image: string;
}
